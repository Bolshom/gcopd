FROM r-base

RUN apt-get update && apt-get install vim -y

COPY . /basedir
WORKDIR /basedir

ENV LC_ALL C.UTF-8
ENV LANG C.UTF-8


RUN useradd -ms /bin/bash user \
	&& chown -R user *
USER user

CMD tail -f /dev/null
